import { UnderDevelopment } from "@/components/under-development";

export default function Page() {
  return (
    <UnderDevelopment
      title="Store Settings"
      description="Configure payment gateways, delivery profiles, site metadata, email SMTP keys, and admin security credentials."
    />
  );
}
